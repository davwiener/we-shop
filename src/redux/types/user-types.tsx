import { Component } from "react";

export interface UserState {
  userName: string;
  connected: boolean;
  popUp: string;
  loaded: boolean;
  isLoading: boolean;
}

//export const OPEN_POPUP = 'OPEN_POPUP';
//export const
